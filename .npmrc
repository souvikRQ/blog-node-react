# Workspace resolution — always use local workspace packages
link-workspace-packages=true
prefer-workspace-packages=true

# Pin the pnpm version via Corepack / package manager field enforcement
manage-package-manager-versions=true

# Use hoisted node_modules so deploy containers without symlink support work correctly
node-linker=hoisted
