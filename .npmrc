# Tells pnpm to always resolve the lockfile and workspace from the root,
# even when a sub-package directory is the working directory during deployment.
link-workspace-packages=true
prefer-workspace-packages=true
